<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Receipt</title>
    <link rel="stylesheet" href="/css/receipt.css" />
  </head>
  <body>
    <div class="receipt-box">
      <header>
        <img
          src="https://res.cloudinary.com/dwmeclnky/image/upload/v1747305009/dhs-new-logo_kmxqfk.png"
          alt="DHS Logo"
          class="logo"
        />
        <div class="header-text">
          <h1>DEFENCE HABITAT HOUSING CO-OPERATIVE SOCIETY LTD.</h1>
          <p>
            Behind Swathi Garden Hotel, E-Block, Sahakar Nagar, Bangalore –
            560092
          </p>
          <p>Mobile: +91 7026147770</p>
        </div>
      </header>

      <section class="receipt-header">
        <div>
          No. <strong class="highlight-red"><%= receiptNumber %></strong>
        </div>
        <div class="receipt">RECEIPT</div>
        <div>Date : <strong><%= date %></strong></div>
      </section>

      <section class="details">
        <p>Received from Smt./Sri : <strong><%= name %></strong></p>
        <p>Address : <strong><%= address %></strong></p>
        <p>Rupees : <strong><%= amountInWords %></strong></p>
        <p>Payment Mode : <strong><%= paymentMode%></strong></p>
        <p>
          RTGS/CHEQUES/DD No :
          <strong
            ><%= chequeNumber || ddNumber || transactionId || "-" %></strong
          >
        </p>
        <p>Bank : <strong><%= bankName || "-" %></strong></p>
        <p>Branch : <strong><%= branchName || "-" %></strong></p>
        <% if (projectName) { %>
        <p>
          Project Name: <strong class="capitalize"><%= projectName %></strong>
        </p>
        <% } %> <% if (projectName) { %>
        <p>Plot Dimension: <strong><%= plotDimension %></strong></p>
        <% } %>

        <!-- <p>Amount : <strong><%= total %></strong></p> -->
      </section>

      <% if (items.length > 0) { %>
      <table class="fee-table">
        <thead>
          <tr>
            <th>Sl No</th>
            <th>Particulars</th>
            <th>L.F</th>
            <th>Rs.</th>
            <th>Ps.</th>
          </tr>
        </thead>
        <tbody>
          <!-- <% items.forEach((item, idx) => { %>
          <tr>
            <td><%= idx + 1 %></td>
            <td><%= item.name %></td>
            <td>..................</td>
            <td><%= item.amount %></td>
            <td>00</td>
          </tr>
          <% }); %> -->
          <% items.forEach((item, idx) => { %>
          <tr>
            <td><%= idx + 1 %></td>
            <td><%= item.name %></td>
            <td>..................</td>
            <td><%= item.amount %></td>
            <td>00</td>
          </tr>
          <% }); %>
        </tbody>
        <tfoot>
          <tr>
            <td colspan="3">Total</td>
            <td><%= total %></td>
            <td>00</td>
          </tr>
        </tfoot>
      </table>
      <% } else { %>
      <p class="no-items">
        No itemized fee breakdown available for this payment type.
      </p>
      <% } %>

      <div class="signature">
        <img
          src="https://res.cloudinary.com/dwmeclnky/image/upload/v1747303140/sign-sec_bzvsze.jpg"
          alt="Signature"
        />
        <div class="footer-role">President / Secretary</div>
      </div>
    </div>
  </body>
</html>
